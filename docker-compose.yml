services:

  mongo:
    image: mongo:8.3.4
    restart: always
    environment:
      MONGO_INITDB_DATABASE: duffDB
    ports:
      - "27017:27017"
    volumes:
      - ./resources/init-mongo.js:/docker-entrypoint-initdb.d/init-mongo.js
