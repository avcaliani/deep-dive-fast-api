name: Setup uv
description: Install uv and sync project dependencies.

runs:
  using: composite
  steps:
    - uses: astral-sh/setup-uv@v8
      with:
        enable-cache: true
    - run: uv sync
      shell: bash
